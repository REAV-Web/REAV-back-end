git add .
git commit -m "test - enable cors"
git push origin master 


서버 애플리케이션을 개발할 떄는 멤버 변수들을 모두 private이라 하고, 멤버 변수에 접근해서 값을 얻고 저장할 getter, setter를 만들자

cors 적용 여부 테스트: https://cors-test.codehappy.dev/



        try{
            URL url = new URL("http://17a1-59-12-219-18.ngrok.io/" + review);
            LOG.info(url + " ");
            LOG.info(url + " ");
            LOG.info(url + " ");
            HttpURLConnection conn = (HttpURLConnection) url.openConnection();
            conn.setRequestMethod("GET");
            BufferedReader in = new BufferedReader(new InputStreamReader(conn.getInputStream(), "UTF-8"));
            String line;
            while((line = in.readLine()) != null) { // response를 차례대로 출력
                LOG.info(url + " : " + line);
                LOG.info(url + " : " + line);
                LOG.info(url + " : " + line);
            }
            weight = Double.parseDouble(line);
        }catch(Exception e){
            e.printStackTrace();
        }